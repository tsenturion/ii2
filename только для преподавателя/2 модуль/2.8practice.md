## Что мы будем делать

Сейчас я построю небольшой, но полностью настоящий GitHub-процесс.

У нас уже существует локальный проект:

```text
hermes-multiagent
```

Сначала я проверю GitHub CLI и авторизацию. Затем создам или подключу удалённый GitHub-репозиторий. После этого подготовлю GitHub Actions вместе с изменением кода в отдельной ветке, чтобы вся практика осталась одним коммитом.

Только после этого я передам работу Hermes. Агент должен будет создать отдельную ветку, выполнить небольшое изменение, проверить его локально, создать commit, отправить ветку на GitHub и открыть Pull Request.

После создания PR работа агента ещё не закончится. Он должен получить состояние CI непосредственно с GitHub и сообщить, выполняется проверка, завершилась успешно или упала.

## Проверяем GitHub CLI

Открываю Hermes Desktop и наш Project:

```text
dependency-lab
```

Справа открываю Terminal сочетанием:

```text
Ctrl + `
```

Проверяю текущую директорию:

```powershell
Get-Location
```

Последний сегмент пути должен называться `hermes-multiagent`. Сохраняю корни
проекта и Git-репозитория в переменные:

```powershell
$projectRoot = (Get-Location).Path
$repoRoot = (git rev-parse --show-toplevel).Trim()
```

Теперь проверяю GitHub CLI:

```powershell
gh --version
```

Если отображается версия, продолжаю.

Если Windows сообщает, что команда `gh` не найдена, открываю обычный PowerShell и выполняю:

```powershell
winget install --id GitHub.cli --source winget
```

После установки полностью закрываю окно терминала и открываю новое. Официальная инструкция GitHub отдельно предупреждает, что установщик изменяет `PATH`, поэтому простого открытия новой вкладки Windows Terminal может быть недостаточно.

После этого снова проверяю:

```powershell
gh --version
```

## Авторизуем GitHub CLI

Теперь выполняю:

```powershell
gh auth status
```

Если авторизация уже есть, GitHub CLI покажет активный аккаунт.

Если авторизации нет, выполняю:

```powershell
gh auth login
```

Дальше появляется интерактивный выбор.

Я выбираю:

```text
GitHub.com
```

Затем выбираю:

```text
HTTPS
```

На вопрос об аутентификации Git выбираю вариант с использованием учётных данных GitHub.

Для входа выбираю:

```text
Login with a web browser
```

GitHub CLI покажет одноразовый код и предложит открыть браузер.

Нажимаю Enter.

В браузере открывается GitHub. Если аккаунт ещё не авторизован, выполняю вход.

В поле устройства ввожу код, который показал `gh`.

Нажимаю кнопку подтверждения авторизации.

Возвращаюсь в PowerShell и снова выполняю:

```powershell
gh auth status
```

GitHub CLI официально рекомендует именно эту проверку перед работой с PR, и Hermes GitHub Skill также выполняет `gh auth status` как предварительный этап.

## Проверяем локальный Git

Работаю из уже открытого корня проекта. Git-команды автоматически находят
родительский репозиторий; при необходимости его путь доступен в `$repoRoot`.

Проверяю текущую ветку:

```powershell
git branch --show-current
```

Для занятия основной веткой будем считать:

```text
main
```

Если текущая ветка называется `master`, можно переименовать:

```powershell
git branch -M main
```

Теперь:

```powershell
git status
```

Перед настройкой удалённого репозитория предыдущие учебные изменения должны быть
сохранены. Не использую `git add .`: в общей рабочей области могут находиться
материалы других модулей. Добавляю только явно относящиеся к практике файлы.

Проверяю:

```powershell
git status
```

Отсутствие посторонних изменений проверяю по выводу. Известные пользовательские
файлы других модулей не добавляю и не изменяю.

## Проверяем наличие GitHub remote

Выполняю:

```powershell
git remote -v
```

Если уже есть:

```text
origin
```

с адресом GitHub, новый репозиторий создавать не нужно.

Дополнительно можно проверить:

```powershell
gh repo view
```

Если GitHub CLI правильно определяет репозиторий, переходим дальше.

Если `origin` отсутствует, создадим учебный репозиторий прямо через CLI.

## Создаём GitHub-репозиторий, если его ещё нет

Выполняю из корня Git-репозитория:

```powershell
Set-Location $repoRoot
gh repo create hermes-multiagent-lab --private --source . --remote origin --push
Set-Location $projectRoot
```

GitHub CLI создаст приватный репозиторий в текущем аккаунте, добавит его как `origin` и отправит текущую ветку.

Проверяю:

```powershell
git remote -v
```

Затем:

```powershell
gh repo view
```

Если хочу открыть репозиторий в браузере:

```powershell
gh repo view --web
```

Откроется страница GitHub.

Теперь наш локальный Git связан с реальным Git-хостингом.

## Создаём отдельный материал для CI

Чтобы результат занятия не зависел от состояния всех предыдущих файлов проекта, я сделаю маленький изолированный пример.

В Hermes Desktop возвращаюсь в центральный чат и отправляю:

```text
В текущем проекте создай каталог ci_lab.

В нём создай файл text_utils.py со следующим содержимым:

def normalize_username(name: str) -> str:
    return name.strip().lower()

Также создай каталог ci_lab/tests и файл ci_lab/tests/test_text_utils.py:

import unittest

from ci_lab.text_utils import normalize_username


class TextUtilsTests(unittest.TestCase):

    def test_trim_and_lowercase(self):
        self.assertEqual(
            normalize_username("  Alice  "),
            "alice",
        )


if __name__ == "__main__":
    unittest.main()

Никаких других файлов не изменяй.
```

Теперь создаю пустой:

```text
ci_lab/__init__.py
```

и:

```text
ci_lab/tests/__init__.py
```

Проверяю локально:

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s ci_lab\tests -v
```

Тест должен пройти.

## Создаём GitHub Actions workflow

Workflow GitHub Actions всегда находится в `.github/workflows` относительно
корня Git-репозитория. В нашей структуре учебный Python-проект вложен в общий
репозиторий, поэтому сам workflow находится рядом с каталогом
`hermes-multiagent`, а команда тестирования получает явную рабочую директорию.

В центральном чате Hermes отправляю:

```text
Создай файл:

.github/workflows/ci.yml

со следующим содержимым.

Другие файлы не изменяй.
```

Содержимое:

```yaml
name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: hermes-multiagent

    steps:
      - name: Получить исходный код
        uses: actions/checkout@v7

      - name: Настроить Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Запустить тесты CI-практики
        run: python -m unittest discover -s ci_lab/tests -v
```

Здесь ключевая часть:

```yaml
on:
  pull_request:
```

означает, что workflow будет реагировать на события Pull Request.

GitHub рекомендует для Python workflows использовать `actions/checkout`, `actions/setup-python` и затем запускать обычную команду тестирования проекта.

## Проверяем подготовленные изменения

CI и учебный пакет пока остаются незакоммиченными. Они будут перенесены в
feature-ветку и войдут в единственный коммит практики вместе с изменением
`normalize_username`.

В Terminal выполняю:

```powershell
git status --short
```

Проверяю:

```powershell
git status
```

Посторонние файлы не добавляю. На этом этапе коммит и push в `main` не выполняю.

## Проверяем Actions через интерфейс GitHub

Открываю репозиторий:

```powershell
gh repo view --web
```

В браузере в верхнем меню репозитория нахожу вкладку:

```text
Actions
```

Нажимаю её.

До публикации feature-ветки новый workflow может ещё не отображаться. После
создания Pull Request в списке появится:

```text
CI
```

Сейчас отсутствие запуска может быть нормальным, потому что наш workflow настроен только на:

```text
pull_request
```

Он запустится после создания PR.

## Проверяем встроенный GitHub Skill Hermes

Возвращаюсь в Hermes Desktop.

Создаю новый чат, чтобы старые экспериментальные инструкции не влияли на работу.

В поле сообщения можно проверить доступность GitHub Skill через slash-команду:

```text
/github-pr-workflow
```

Hermes регистрирует установленные Skills как slash-команды; встроенный `github-pr-workflow` предназначен для полного жизненного цикла PR: branch, commit, open PR, CI и merge.

Если команда загрузилась, Hermes уже имеет специализированные инструкции по GitHub workflow.

Но конкретную задачу я пока не даю.

## Создаём задачу для Pull Request

Наша задача будет небольшой.

Исходная функция:

```python
def normalize_username(name: str) -> str:
    return name.strip().lower()
```

Мы хотим, чтобы пробелы внутри имени пользователя превращались в символ `_`.

Например:

```text
"  Alice Smith  "
```

должно превращаться в:

```text
alice_smith
```

Агент также должен добавить тест этого поведения.

Это достаточно маленькое изменение, чтобы основное внимание занятия осталось на PR и CI.

## Передаём задачу Hermes

В новом чате Hermes отправляю:

```text
Работаем с текущим GitHub-репозиторием.

Выполни задачу полностью через GitHub PR workflow.

Требование к коду:

Функция ci_lab/text_utils.py:

normalize_username(name)

должна после удаления внешних пробелов и преобразования строки в нижний регистр также заменять внутренние пробелы символом подчёркивания.

Пример:

"  Alice Smith  "

должно стать:

"alice_smith"

Добавь тест этого поведения.

Требования к Git-процессу:

сначала проверь gh auth status и состояние репозитория;

убедись, что работа начинается с main;

не изменяй main напрямую;

создай отдельную ветку:

feature/normalize-username

включи в единственный коммит практики уже подготовленные ci_lab и CI workflow;

не добавляй пользовательские файлы других модулей;

выполни только необходимые изменения;

запусти локальные тесты;

проверь git diff;

создай commit с понятным сообщением;

отправь ветку в origin;

создай Pull Request в main через GitHub CLI;

после создания PR получи его номер и URL;

не выполняй merge;

после открытия PR отдельно проверь состояние CI через gh pr checks;

в конце сообщи:

имя ветки;
commit;
номер PR;
URL PR;
текущий статус CI.

Не утверждай, что CI прошёл успешно, пока это не подтверждено GitHub.
```

Теперь Hermes должен выполнить последовательность команд самостоятельно.

Я не ввожу эти Git-команды вручную. В этом и заключается практическая часть: агент работает как исполнитель PR-процесса.

## Наблюдаем первые действия агента

Я слежу за карточками Terminal Tool в Hermes.

Сначала ожидаю проверку:

```text
gh auth status
```

Затем:

```text
git status
```

и определение текущей ветки.

Hermes GitHub Skill прямо требует предварительно проверять `gh auth status`, а удалённое состояние получать свежими вызовами `gh`, а не предполагать его.

После этого агент должен перейти на `main`, при необходимости получить актуальную версию и создать рабочую ветку.

Я ожидаю что-то эквивалентное:

```text
git switch main
```

```text
git pull --ff-only
```

```text
git switch -c feature/normalize-username
```

Названия фактических команд могут немного различаться. Нас интересует результат: изменение не должно выполняться непосредственно в `main`.

## Проверяем создание ветки в интерфейсе Hermes

После того как агент сообщил о создании ветки, открываю справа Terminal и самостоятельно выполняю:

```powershell
git branch --show-current
```

Должно быть:

```text
feature/normalize-username
```

Теперь:

```powershell
git status
```

Во время работы Coder могут появляться изменённые файлы.

## Наблюдаем изменение и локальную проверку

Hermes должен изменить:

```text
ci_lab/text_utils.py
```

и:

```text
ci_lab/tests/test_text_utils.py
```

После изменений ожидаю запуск:

```text
python -m unittest discover -s ci_lab/tests -v
```

или эквивалентной команды через `.venv`.

Важно, чтобы локальный тест прошёл до создания PR.

Затем агент должен посмотреть:

```text
git diff
```

Только после анализа diff он должен создать commit.

## Проверяем commit

После того как Hermes сообщил о commit, в Terminal выполняю:

```powershell
git log -1 --oneline
```

Я должен увидеть новый commit.

Например:

```text
abc1234 Add username space normalization
```

Затем:

```powershell
git status
```

Рабочая директория должна быть чистой.

## Наблюдаем push

Hermes должен отправить ветку:

```text
feature/normalize-username
```

в:

```text
origin
```

Обычно для первого push используется:

```text
git push -u origin feature/normalize-username
```

После этого локальная ветка получает upstream.

Проверяю:

```powershell
git branch -vv
```

Возле текущей ветки должна отображаться связь с:

```text
origin/feature/normalize-username
```

## Наблюдаем создание Pull Request

Следующим действием Hermes должен использовать GitHub CLI.

GitHub CLI официально создаёт PR через:

```text
gh pr create
```

Для указания целевой и исходной веток используются `--base` и `--head`; заголовок и описание можно передать через `--title` и `--body`.

Например, агент может выполнить команду примерно такого вида:

```text
gh pr create --base main --head feature/normalize-username --title "Normalize spaces in usernames" --body "..."
```

После успешного создания `gh` возвращает URL Pull Request.

## Проверяем PR вручную через GitHub

После того как Hermes сообщает URL, я нажимаю ссылку в его ответе.

Если ссылка не открывается из интерфейса, выполняю:

```powershell
gh pr view --web
```

GitHub CLI официально поддерживает `gh pr view --web` для открытия PR текущей ветки в браузере.

В браузере я оказываюсь на странице Pull Request.

В верхней части вижу заголовок PR.

Под ним вижу направление:

```text
feature/normalize-username
```

в:

```text
main
```

Ниже открываю вкладку:

```text
Files changed
```

Проверяю, что изменены только нужные файлы.

Возвращаюсь на:

```text
Conversation
```

В нижней части страницы должен появиться блок автоматических checks.

## Что происходит сразу после открытия PR

GitHub видит событие:

```text
pull_request
```

и запускает `.github/workflows/ci.yml`.

Поэтому первые несколько секунд или минут статус может быть не зелёным и не красным, а:

```text
queued
```

или:

```text
in progress
```

Это нормальное состояние.

Задача агента — не угадать итог, а получить реальный серверный статус.

## Hermes проверяет CI

После создания PR в нашем первоначальном промпте уже было требование получить checks.

Hermes должен вызвать:

```text
gh pr checks
```

Если номер PR известен, может использовать:

```text
gh pr checks <номер>
```

Команда `gh pr checks` специально показывает CI-состояние одного Pull Request. Она различает состояния `pass`, `fail`, `pending`, `skipping` и `cancel`. Если аргумент не передан, CLI использует PR текущей ветки.

Если CI ещё выполняется, правильный ответ агента должен быть примерно:

```text
CI: pending
```

а не:

```text
CI успешно пройден.
```

## Просим Hermes дождаться завершения checks

Если в первом ответе CI ещё находится в состоянии pending, отправляю следующее сообщение:

```text
Продолжи работу с тем же Pull Request.

Не изменяй код.

Отслеживай CI до завершения через GitHub CLI.

После завершения сообщи фактический результат checks.

PR не объединяй.
```

Hermes может использовать:

```text
gh pr checks --watch
```

Эта команда остаётся активной до завершения проверок. По умолчанию состояние обновляется каждые 10 секунд; также поддерживается `--fail-fast`.

После завершения я ожидаю один из двух реальных результатов:

```text
pass
```

или:

```text
fail
```

## Проверяем CI самостоятельно

Теперь я не ограничиваюсь сообщением агента.

В Hermes Terminal выполняю:

```powershell
gh pr checks
```

Если всё прошло правильно, вижу успешный check workflow `CI`.

Можно получить более структурированный вывод:

```powershell
gh pr checks --json name,state,bucket,workflow
```

Поле `bucket` особенно удобно, потому что GitHub CLI нормализует множество внутренних состояний в категории:

```text
pass
fail
pending
skipping
cancel
```

## Открываем checks в браузере

Для визуальной демонстрации студентам выполняю:

```powershell
gh pr checks --web
```

Браузер открывает страницу проверок PR.

Либо на странице самого Pull Request нахожу блок:

```text
Checks
```

и нажимаю:

```text
Details
```

возле workflow.

GitHub переносит меня в GitHub Actions.

Здесь можно раскрыть job:

```text
tests
```

Затем увидеть отдельные steps:

```text
Checkout repository
Set up Python
Run tests
```

Таким образом, мы видим не просто зелёную галочку, а фактический серверный процесс.

## Проверяем workflow через вкладку Actions

Есть ещё один способ.

На странице репозитория в верхнем меню нажимаю:

```text
Actions
```

В списке workflows выбираю:

```text
CI
```

Нахожу запуск, связанный с нашей веткой или Pull Request.

Открываю его.

Внутри нажимаю job:

```text
tests
```

Теперь вижу полный лог CI.

Это особенно полезно, если checks завершились с ошибкой.

## Получаем статус PR целиком

Возвращаюсь в Hermes и отправляю:

```text
Получи текущее состояние созданного Pull Request через GitHub CLI.

Покажи:

номер;
state;
base branch;
head branch;
URL;
состояние CI.

Ничего не изменяй и PR не объединяй.
```

Hermes может использовать:

```text
gh pr view
```

и:

```text
gh pr checks
```

`gh pr status` также показывает краткую сводку по PR, включая состояние CI и review.

## Почему не создаём искусственное падение CI

В исходном варианте задания предлагалось намеренно сломать тест, отправить ещё
один коммит, изучить ошибку CI и затем создать третий исправляющий коммит. Это
полезный отдельный эксперимент, но он противоречит правилу курса «одна практика —
один коммит».

В этой работе ограничиваемся одним корректным коммитом и фактическим успешным
запуском CI. Если реальная серверная проверка завершится ошибкой, использую
`gh pr checks`, `gh run view --log-failed` и журналы GitHub Actions для поиска
причины. Чтобы сохранить один итоговый коммит, исправление в этой новой
feature-ветке можно включить через `git commit --amend --no-edit` и опубликовать
командой `git push --force-with-lease`. Такой способ применим только к созданной
для практики ветке, которой больше никто не пользуется. Искусственную ошибку в
удалённый PR не отправляю.

## Проверяем, что Hermes не выполнил merge

В Terminal выполняю:

```powershell
gh pr view --json state,mergedAt,mergeStateStatus,url
```

PR должен оставаться открытым, а:

```text
mergedAt
```

не должен содержать дату слияния.

Это важная граница нашей практики.

Создать PR и проверить CI агенту разрешено.

Объединять изменение мы ему не поручали.

## Почему мы не заканчиваем после gh pr create

Теперь я фиксирую основную идею занятия.

Команда:

```text
gh pr create
```

говорит только о том, что объект Pull Request успешно создан на GitHub. Она ничего не доказывает о качестве кода.

Даже успешный локальный тест не подтверждает серверный CI.

И только:

```text
gh pr checks
```

даёт агенту текущее состояние проверок GitHub.

Это соответствует дисциплине встроенного GitHub Skill Hermes: удалённое состояние необходимо читать свежим запросом, а не выводить из локального состояния или прошлого сообщения.

## Что мы фактически сделали

В этой практике Hermes начал работу с локального Git-репозитория, но закончил уже внутри реального GitHub-процесса.

Агент создал отдельную feature-ветку и одним коммитом добавил CI workflow,
учебный пакет, изменение функции и тесты.

Он изменил код и тест.

Локально проверил результат.

Создал commit.

Отправил ветку на GitHub.

Через `gh pr create` создал Pull Request. GitHub CLI официально поддерживает создание PR с указанием base/head, title и body.

Событие Pull Request запустило GitHub Actions.

После этого Hermes отдельно запросил серверное состояние через `gh pr checks`.

Мы получили фактический успешный статус CI и сохранили Pull Request открытым для
проверки без автоматического merge.

В результате агент здесь уже работает не просто с кодом. Он становится участником полноценного процесса разработки: локальный Git, удалённая ветка, GitHub Pull Request и внешний CI образуют единый рабочий цикл.
